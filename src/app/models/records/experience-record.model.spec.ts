import { ExperienceRecordModel } from './experience-record.model';

describe('ExperienceRecordModel', () => {
  it('should create an instance', () => {
    expect(new ExperienceRecordModel()).toBeTruthy();
  });
});
