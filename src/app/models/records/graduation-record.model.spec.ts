import { GraduationRecordModel } from './graduation-record.model';

describe('GraduationRecordModel', () => {
  it('should create an instance', () => {
    expect(new GraduationRecordModel()).toBeTruthy();
  });
});
